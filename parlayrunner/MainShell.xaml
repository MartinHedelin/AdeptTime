<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="AdeptTime.MainShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:AdeptTime.Views"
    xmlns:components="clr-namespace:AdeptTime.Components"
    Shell.NavBarIsVisible="False"
    BackgroundColor="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Content Area -->
        <ContentView x:Name="ContentArea" Grid.Row="0" />

        <!-- Custom Bottom Navigation -->
        <components:CustomBottomNavigation 
            Grid.Row="1" 
            x:Name="BottomNavigation"
            TabSelected="OnTabSelected" />
    </Grid>
</ContentPage>
